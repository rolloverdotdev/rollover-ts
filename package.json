{
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "generate": "openapi-ts",
    "build": "npm run build -w packages/client && npm run build -w packages/browser"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "0.95.0",
    "@x402/fetch": "*",
    "@x402/evm": "*",
    "viem": "^2.0.0",
    "typescript": "6.0.2"
  }
}
