{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/server && npm run build -w packages/browser"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@x402/evm": "*",
    "@x402/fetch": "*",
    "typescript": "6.0.2",
    "viem": "^2.0.0"
  }
}
